/target
/target-*

# Local run config (may hold a real devin token — never commit)
config.yaml

# Build/packaging outputs
/dist/
/devin-2api
/devin-2api.exe
/devin-2api-linux-*

# Runtime state symlink / dir created by deploy scripts
/logs

# Test and QA outputs
/test-results/
/outputs/perf/
/outputs/bench/

# Node tooling
/node_modules

.DS_Store

# PGO working dirs (merged.profdata + profile.meta are checked in)
/pgo/profraw/
/pgo/qa-stress/
/pgo/work/
/pgo/pgo-warnings.log
