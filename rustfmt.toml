# Formatting policy: `cargo fmt --all -- --check` is the CI gate.
# Only stable rustfmt options are used so the pinned stable toolchain applies.
style_edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
