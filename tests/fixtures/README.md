# tests/fixtures

Shared test fixtures for the parity suite. Owning tasks add fixture files
here (request/response corpora, wire captures, descriptor sets); fixtures
are data only — no credentials, no live-upstream captures with secrets.

Task 2 establishes the directory; protocol fixtures land with tasks 3+.
